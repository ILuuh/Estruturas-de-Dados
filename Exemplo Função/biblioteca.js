function soma(p1, p2) {
    var res = p1 + p2;
    return res
}

function subtrair(p1, p2) {
    var res = p1 - p2;
    document.write("<br>" + `Subtração: ${v1} - ${v2} = ${res}`)
}

function multi(p1, p2) {
    var res = p1 * p2;
    return res
}

function divi(p1, p2) {
    var res = p1 / p2;
    return res
}